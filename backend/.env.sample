# Backend environment sample (safe to commit)
# Copy to .env and fill values before running locally or in Docker.
MONGO_URI=
JWT_SECRET=
ACCESS_TOKEN_EXPIRES=7d
NODE_ENV=development
PORT=5000
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=
ALLOW_REGISTRATION=false
CORS_ORIGIN=http://localhost:3000
COOKIE_SECURE=
SMTP_USER=
SMTP_PASS=
